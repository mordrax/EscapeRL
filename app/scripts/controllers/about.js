'use strict';

/**
 * @ngdoc function
 * @name escapeRlApp.controller:AboutCtrl
 * @description
 * # AboutCtrl
 * Controller of the escapeRlApp
 */
angular.module('escapeRlApp')
  .controller('AboutCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
